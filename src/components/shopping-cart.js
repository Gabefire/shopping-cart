import "../styles/shopping-cart.css";

const ShoppingCart = ({ store, cart }) => {
  return (
    <div className="shopping-cart">
      <h2>Shopping Cart</h2>
    </div>
  );
};

export default ShoppingCart;
